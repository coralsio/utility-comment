<?php

namespace Tests\Feature;

use Illuminate\Foundation\Testing\DatabaseTransactions;
use Tests\TestCase;

class UtilityCommentViewTest extends TestCase
{
    use DatabaseTransactions;

    protected function setUp(): void
    {
        parent::setUp(); // TODO: Change the autogenerated stub

        $this->post('login', [
            'email' => 'superuser@corals.io',
            'password' => '123456',
        ]);
    }

    /**
     * A basic test example.
     *
     * @return void
     */
    public function test_utility_comment_view()
    {
        $response = $this->get('utilities/comments');

        $response->assertStatus(200)->assertViewIs('utility-comment::index');
    }

    public function test_utility_comment_bulk_action()
    {
        $response = $this->post('utilities/comments/bulk-action', [
            'action' => 'trashed',]);


        $response->assertSeeText('message');
    }
}
